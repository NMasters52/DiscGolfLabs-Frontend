# Routes — Frontend

> Status: **stub** — map of React Router routes. Declared in `app/routes.ts`; tree under `app/routes/`.

## Areas
- `_landing/` — public marketing pages (hero, features, stats, FAQ, pricing, waitlist).
- `app/` — authenticated app (incl. `dashboard/`).
- `courses/` — course learning pages.
- `checkout/` — Stripe checkout flow.
- `welcome/` — post-auth welcome.

## To document
- Full route → file map, each with auth requirement (public/protected) and primary loader/action.
